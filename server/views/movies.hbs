{{> header}}
{{> menu}}

<h2>Pel·lícules (15 primeres)</h2>

<div class="movie-list">
    {{#each movies}}
    <div class="movie-card">
        <img src="/imatges/movie{{film_id}}.png" alt="{{title}}">
        <h3>{{title}} ({{release_year}})</h3>
        <p>{{description}}</p>
        <p><strong>Actors:</strong> {{actors}}</p>
    </div>
    {{/each}}
</div>

{{> footer}}